	
	<section id="brain">
		<h2 id="pTitle">¡Atr&eacute;vete a iniciar!</h2>
		<h3 id="descTitle"></h3>
		
		<% @playlist.videos.each do |video|%>
			<a href="/video/<%= video.unique_id %>"><img src="<%= video.thumbnails[1].url %>" style='width:20%;'></a>
		<%end%>
	</section>

